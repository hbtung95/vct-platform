// Legacy compatibility shim.
// Keep this file so older imports resolving to `.tsx` continue to work,
// while the canonical registry lives in `screen-registry.ts`.

export * from './screen-registry'
